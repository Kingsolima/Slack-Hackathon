# Slack
SLACK_BOT_TOKEN=xoxb-...
SLACK_SIGNING_SECRET=...
SLACK_ADMIN_USER_ID=U...       # Your Slack user ID to receive admin DMs
SECURITY_CHANNEL_ID=C...       # Channel that receives the weekly threat report
FIREWALL_REPORT_TIMEZONE=America/Toronto

# Supabase
SUPABASE_URL=https://xgtwrgfskbscpilofqth.supabase.co
SUPABASE_SERVICE_ROLE_KEY=...  # NOT the publishable key — get this from Supabase dashboard > Settings > API

# Reasoning engine (the AI pipeline service)
OMAR_PIPELINE_URL=http://localhost:8001  # where the proxy reaches the pipeline; update for Railway
PIPELINE_MODEL=claude-haiku-4-5          # optional override; defaults to claude-haiku-4-5 (fast, accurate for these tasks)

# Claude API — required by the reasoning engine (src/pipeline)
ANTHROPIC_API_KEY=sk-ant-...

# App
PORT=8000
